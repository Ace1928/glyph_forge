{
  "name": "glyph-forge-web-quality",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "description": "Cross-browser production checks for Glyph Forge Studio",
  "scripts": {
    "test:web": "playwright test",
    "test:web:chromium": "playwright test --project=desktop-chromium --project=mobile-chrome"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@axe-core/playwright": "4.12.1",
    "@playwright/test": "1.62.1"
  }
}
