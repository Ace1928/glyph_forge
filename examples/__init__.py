"""Importable example plugins and API samples.

Making this a regular package keeps ``examples.plugin_example`` importable
even when an unrelated ``examples`` module exists earlier on ``sys.path``.
None of the samples are part of the installed distribution.
"""
